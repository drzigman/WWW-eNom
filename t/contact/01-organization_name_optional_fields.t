#!/usr/bin/env perl

use strict;
use warnings;

use Test::More;
use Test::Exception;

use WWW::eNom::Contact;

subtest 'No Organization Name' => sub {
    fail( 'TODO' );
};

subtest 'With Organization Name' => sub {
    fail( 'TODO' );
};

done_testing;
